import { BooleanATextoDirective } from './boolean-a-texto.directive';

describe('BooleanATextoDirective', () => {
  it('should create an instance', () => {
    const directive = new BooleanATextoDirective();
    expect(directive).toBeTruthy();
  });
});
